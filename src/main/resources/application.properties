# ===============================================
# Configurația Bazei de Date MySQL (RAILWAY LIVE)
# ===============================================

# 1. URL-ul de conexiune către Railway (Public URL)
spring.datasource.url=jdbc:mysql://trolley.proxy.rlwy.net:45583/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# 2. CREDENȚIALE RAILWAY
spring.datasource.username=root
spring.datasource.password=bOdNreJEzoSitlJeIjsNvlTqCZLIjWFL

# 3. Driver-ul necesar
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================================
# Configurația JPA și Hibernate
# ===============================================

# Afișează interogările SQL în consolă pentru a vedea ce se întâmplă
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Setează dialectul pentru MySQL 8 (folosit de Railway)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# "update" creează tabelele automat dacă ele nu există
spring.jpa.hibernate.ddl-auto=update

# ===============================================
# Configurația Serverului Web (Tomcat)
# ===============================================

server.port=8080